import { TamañoArchivoPipe } from './tamaño-archivo.pipe';

describe('TamañoArchivoPipe', () => {
  it('create an instance', () => {
    const pipe = new TamañoArchivoPipe();
    expect(pipe).toBeTruthy();
  });
});
